% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hs2mm.R
\name{hs2mm}
\alias{hs2mm}
\title{Convert human Entrez Gene IDs to mouse ortholog IDs}
\usage{
hs2mm(entrezid, ...)
}
\arguments{
\item{entrezid}{A character, numeric, or factor vector of source Entrez Gene
IDs. Character input is recommended. Leading/trailing whitespace is trimmed
for lookup, and only positive integer strings are accepted after conversion
to character. Gene symbols, prefixed IDs, decimal strings, and obsolete IDs
are not automatically converted. \code{NA} and blank strings are missing inputs.}

\item{...}{Additional arguments passed to \code{\link[=find_orthologs]{find_orthologs()}}, such as
\code{details}, \code{cache_dir}, \code{refresh}, \code{orthologs}, and \code{method}. Do not supply
\code{from_taxid} or \code{to_taxid}; they are fixed by this wrapper.}
}
\value{
The named character vector or detailed data frame described in
\code{\link[=find_orthologs]{find_orthologs()}}. Multiple mouse candidates yield \code{NA} by default;
\code{details = TRUE} retains all candidates.
}
\description{
Convenience wrapper around \code{\link[=find_orthologs]{find_orthologs()}} with human (\code{9606}) as the
source species and mouse (\code{10090}) as the target species.
}
\examples{
# Synthetic reference for an offline example.
ref <- data.frame(source_entrezid = "1", target_entrezid = "11")
hs2mm("1", orthologs = ref)
hs2mm(c("1", NA), orthologs = ref, details = TRUE)
\dontrun{
hs2mm(c("7157", "1956", "3569"))
hs2mm(c("7157", "1956", "3569"), details = TRUE)
}
}
\seealso{
\code{\link[=find_orthologs]{find_orthologs()}}, \code{\link[=load_orthologs]{load_orthologs()}}, \code{\link[=read_orthologs]{read_orthologs()}}
}
