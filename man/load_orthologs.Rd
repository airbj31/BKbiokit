% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hs2mm.R
\name{load_orthologs}
\alias{load_orthologs}
\title{Download or reuse an NCBI ortholog reference}
\usage{
load_orthologs(
  from_taxid,
  to_taxid,
  cache_dir = "data/reference/ncbi_orthologs",
  refresh = FALSE,
  source_file = NULL,
  method = c("shared_anchor", "direct")
)
}
\arguments{
\item{from_taxid, to_taxid}{Single positive integer or digit string containing
an NCBI taxonomy ID. For example, human is \code{9606}, mouse is \code{10090}, and
rat is \code{10116}. Source and target must differ; species names are not accepted.}

\item{cache_dir}{Directory for the compressed NCBI snapshot and filtered RDS
caches. Relative paths are resolved against the current working directory.}

\item{refresh}{Single logical value. If \code{TRUE}, download a new snapshot and
rebuild the requested pair cache. If \code{FALSE}, reuse the snapshot when
present. Ignored when \code{source_file} is supplied.}

\item{source_file}{Optional path to a local NCBI file accepted by
\code{\link[=read_orthologs]{read_orthologs()}}. When supplied, bypass downloading and caching.}

\item{method}{Relationship selection method. \code{"shared_anchor"} (default)
includes direct pairs and pairs sharing the same NCBI primary gene.
\code{"direct"} includes only pairs explicitly recorded in the file, accepting
either orientation.}
}
\value{
The reference data frame documented in \code{\link[=read_orthologs]{read_orthologs()}}. When using
the managed cache, the additional \code{snapshot_file} attribute records the
absolute path to the retained compressed snapshot.
}
\description{
Loads a species-pair reference from a local file or a cached NCBI snapshot.
A downloaded compressed snapshot is retained for reuse across species pairs.
}
\details{
The download URL is
\url{https://ftp.ncbi.nlm.nih.gov/gene/DATA/gene_orthologs.gz}.
The raw snapshot is stored as \code{gene_orthologs.gz} in \code{cache_dir}. Filtered
caches are keyed by source/target taxonomy IDs, method, and the raw-file MD5,
preventing reuse of a pair cache from a different snapshot. There is no
automatic periodic refresh. Network, parsing, and file-writing errors are
propagated to the caller.
}
\examples{
\dontrun{
# These calls may download the full NCBI snapshot and write cache files.
ref <- load_orthologs(9606, 10090)
mouse_rat <- load_orthologs(10090, 10116)
refreshed <- load_orthologs(9606, 10090, refresh = TRUE)
}
}
\seealso{
\code{\link[=read_orthologs]{read_orthologs()}}, \code{\link[=find_orthologs]{find_orthologs()}}, \code{\link[=hs2mm]{hs2mm()}}
}
