% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_GO_genes.R
\name{get_gogenes_recursive}
\alias{get_gogenes_recursive}
\title{Extract Genes for a GO Term with Evidence-Based Recursive Mapping}
\usage{
get_gogenes_recursive(
  go_id,
  org_db = org.Mm.eg.db,
  evidence_filter = c("EXP", "IDA")
)
}
\arguments{
\item{go_id}{Character. The parent Gene Ontology ID (e.g., "GO:0002376").}

\item{org_db}{An AnnotationDb object. The organism-specific database
(default: \code{org.Mm.eg.db}).}

\item{evidence_filter}{Character vector. A list of GO evidence codes to include
(e.g., \code{c("EXP", "IDA")} for experimental levels, or \code{c("EXP", "IDA", "IEA")}
to include electronic annotations). If NULL, all evidence codes are included.}
}
\value{
A character vector of unique gene symbols. Returns \code{NULL} if no genes
are found or if an error occurs.
}
\description{
This function retrieves a list of gene symbols associated with a specific GO ID
and all its descendant (offspring) terms. Unlike the standard \code{GOALL} mapping,
this function allows for strict filtering based on Evidence Codes before
aggregating genes across the GO hierarchy.
}
\details{
The standard \code{keytype = "GOALL"} approach relies on a pre-computed table that
merges all evidence types, which can lead to "data dilution" by low-confidence
annotations (like IEA) across all levels.

This function ensures data integrity by:
\enumerate{
\item Identifying all offspring nodes using the \code{GO.db} hierarchy.
\item Querying each node individually using \code{keytype = "GO"} (Direct Annotation).
\item Filtering by the user-defined evidence codes at each step.
}

This is particularly useful for creating multi-omics feature sets (GMT files)
categorized by different biological confidence levels.
}
\examples{
\dontrun{
# Level 1: Experimental evidence only
genes_lvl1 <- get_filtered_recursive_genes("GO:0002376",
                                            evidence_filter = c("EXP", "IDA"))

# Level 4: Including electronic annotations (IEA)
genes_lvl4 <- get_filtered_recursive_genes("GO:0002376",
                                            evidence_filter = c("EXP", "IDA", "IEA"))
}
}
