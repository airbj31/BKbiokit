% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hs2mm.R
\name{find_orthologs}
\alias{find_orthologs}
\title{Find Entrez Gene ID orthologs between two species}
\usage{
find_orthologs(
  entrezid,
  from_taxid,
  to_taxid,
  details = FALSE,
  cache_dir = "data/reference/ncbi_orthologs",
  refresh = FALSE,
  orthologs = NULL,
  method = c("shared_anchor", "direct")
)
}
\arguments{
\item{entrezid}{A character, numeric, or factor vector of source Entrez Gene
IDs. Character input is recommended. Leading/trailing whitespace is trimmed
for lookup, and only positive integer strings are accepted after conversion
to character. Gene symbols, prefixed IDs, decimal strings, and obsolete IDs
are not automatically converted. \code{NA} and blank strings are missing inputs.}

\item{from_taxid, to_taxid}{Single positive integer or digit string containing
an NCBI taxonomy ID. For example, human is \code{9606}, mouse is \code{10090}, and
rat is \code{10116}. Source and target must differ; species names are not accepted.}

\item{details}{Single logical value. If \code{FALSE} (default), return a named
character vector. If \code{TRUE}, return all candidates and input statuses in a
detailed data frame.}

\item{cache_dir}{Directory for the compressed NCBI snapshot and filtered RDS
caches. Relative paths are resolved against the current working directory.}

\item{refresh}{Single logical value. If \code{TRUE}, download a new snapshot and
rebuild the requested pair cache. If \code{FALSE}, reuse the snapshot when
present. Ignored when \code{source_file} is supplied.}

\item{orthologs}{Optional reference data frame, typically produced by
\code{\link[=load_orthologs]{load_orthologs()}} or \code{\link[=read_orthologs]{read_orthologs()}}. Required columns are
\code{source_entrezid} and \code{target_entrezid}, containing nonmissing positive
integer IDs. Optional \code{evidence} and \code{anchor_keys} columns are carried into
detailed output. Loader-generated species attributes are checked against
the requested direction. The caller is responsible for the species,
completeness, and provenance of custom tables.}

\item{method}{Relationship selection method. \code{"shared_anchor"} (default)
includes direct pairs and pairs sharing the same NCBI primary gene.
\code{"direct"} includes only pairs explicitly recorded in the file, accepting
either orientation.}
}
\value{
With \code{details = FALSE}, a character vector of the same length as
\code{entrezid}, named using the original inputs. Missing, invalid, unmapped,
and multiple-candidate inputs return \code{NA}. Unmapped and multiple-candidate
inputs produce a summary warning.

With \code{details = TRUE}, a data frame with one row per input/candidate and
one row for each input without candidates. Columns are:
\itemize{
\item \code{input_row}: one-based input position.
\item \code{input_entrezid}: original input converted to character.
\item \code{source_entrezid}: trimmed lookup ID.
\item \code{target_entrezid}: candidate ID, or \code{NA}.
\item \code{relationship}: \code{"Ortholog"} for a mapped pair, otherwise \code{NA}.
\item \code{status}: \code{"mapped"}, \code{"ambiguous"}, \code{"unmapped"}, \code{"missing_id"}, or
\code{"invalid_id"}. Ambiguous means multiple distinct target candidates.
\item \code{n_target_candidates}: number of distinct target candidates for the input.
\item \code{mapping_type}: \code{"one_to_one"}, \code{"one_to_many"}, \code{"many_to_one"}, or
\code{"many_to_many"}, interpreted source-to-target; \code{NA} without candidates.
\item \code{evidence}, \code{anchor_keys}: provenance from the reference, or \code{NA}.
\item \code{source_taxid}, \code{target_taxid}: requested taxonomy IDs as strings.
}

The \code{reference_metadata} attribute preserves reference attributes other
than its names, row names, and class. Empty input returns an empty vector
or a zero-row detailed table.
}
\description{
Maps source-species Entrez Gene IDs to target-species IDs using an NCBI
reference or a supplied mapping table. Input order and duplicates are retained.
}
\details{
When \code{orthologs} is supplied, its existing relationships are used;
\code{cache_dir}, \code{refresh}, and \code{method} do not reload or filter it. Without a
supplied reference, the function calls \code{\link[=load_orthologs]{load_orthologs()}} only when at least
one input is a valid ID. Sourcing the script itself performs no I/O.

A single target candidate is returned even if another source gene also maps
to that target. The default output therefore does not enforce reciprocal
one-to-one mapping. To select reciprocal unique pairs, use \code{details = TRUE}
and filter \code{mapping_type == "one_to_one"}. Cardinality is calculated across
the entire supplied reference, not only the input subset, and is not a
biological confidence score. Missing reference pairs do not establish that
a biological ortholog is absent.
}
\examples{
# Synthetic reference: no network access or cache writes.
ref <- data.frame(
  source_entrezid = c("1", "2", "2"),
  target_entrezid = c("11", "12", "13")
)
find_orthologs("1", 9606, 10090, orthologs = ref)
find_orthologs(c("1", "2", "99", NA), 9606, 10090,
               orthologs = ref, details = TRUE)
\dontrun{
# Real NCBI lookup; may download and cache a snapshot.
find_orthologs("22059", from_taxid = 10090, to_taxid = 10116,
               details = TRUE)
}
}
\seealso{
\code{\link[=hs2mm]{hs2mm()}}, \code{\link[=load_orthologs]{load_orthologs()}}, \code{\link[=read_orthologs]{read_orthologs()}}
}
